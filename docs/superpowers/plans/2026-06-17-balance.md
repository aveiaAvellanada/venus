# Balance (M11) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir M11 (Balance, solo dueño): estado de flujo de caja del período — ingresos netos (ventas − reembolsos + cobros, por fecha de venta) − egresos (gastos fijos/variables + pagos a proveedores + sueldos, por su fecha de pago), con proyección del mes y navegación histórica.

**Architecture:** Un RPC `SECURITY DEFINER` dueño-only `obtener_balance(p_desde, p_hasta)` que agrega todo (solo lectura). Encima, `lib/balance.ts` (lógica pura de proyección/rangos + acceso a datos) y UI `app/(app)/balance/` de una pantalla. No escribe datos → bajo riesgo.

**Tech Stack:** Supabase (PostgreSQL 17, RPC plpgsql), React Native + Expo SDK 54, TypeScript estricto, jest.

**Spec:** `docs/superpowers/specs/2026-06-17-balance-design.md`
**Rama:** `feat/m11-balance` (creada; spec commiteado). **No mergear a `main` sin aprobación humana.**

---

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `supabase/migrations/<ts>_m11_balance.sql` (nuevo) | RPC `obtener_balance` (no crea tablas) |
| `supabase/tests/smoke_test_balance.sql` (gitignored) | Smoke en transacción con rollback |
| `lib/database.types.ts` (regenerar vía MCP) | Tipos generados |
| `lib/permisos.ts` (modificar) | `ruta: '/balance'` en módulo `balance` |
| `lib/balance.ts` (nuevo) | Lógica pura (proyección, rangos) + acceso a datos |
| `lib/balance.test.ts` (nuevo) | Tests jest de la lógica pura |
| `app/(app)/balance/_layout.tsx` (nuevo) | Stack + gate dueño-only |
| `app/(app)/balance/index.tsx` (nuevo) | Pantalla de balance |
| `lib/balance_ui.test.tsx` (nuevo) | Tests jest de la UI |

---

## Task 1: Escribir la migración (RPC obtener_balance)

**Files:** Create `supabase/migrations/<ts>_m11_balance.sql` (`<ts>` con `date +%Y%m%d%H%M%S`)

- [ ] **Step 1: Confirmar columnas (MCP execute_sql)**
```sql
select table_name, column_name from information_schema.columns
where table_schema='public' and table_name in
('metodos_pago_venta','ventas','devoluciones','gastos_fijos_pagos','gastos_variables','compra_pagos','empleado_pagos')
order by table_name, ordinal_position;
```
Esperado: metodos_pago_venta(venta_id,metodo,monto); ventas(id,estado,created_at); devoluciones(venta_id,monto_devuelto,monto_cobrado); gastos_fijos_pagos(monto_pagado,fecha_pago); gastos_variables(monto,fecha); compra_pagos(monto,fecha); empleado_pagos(monto,fecha_pago). Ajustar si difiere.

- [ ] **Step 2: Escribir la migración**, contenido exacto:

```sql
-- M11 Balance: RPC de agregación de flujo de caja del período (solo dueño, solo lectura).
-- No crea tablas.

create or replace function public.obtener_balance(p_desde date, p_hasta date)
returns json
language plpgsql
security definer
set search_path = ''
as $$
declare
  v_ef numeric := 0; v_ne numeric := 0; v_da numeric := 0;
  v_reemb numeric := 0; v_cobros numeric := 0;
  v_gf numeric := 0; v_gv numeric := 0; v_prov numeric := 0; v_sueldos numeric := 0;
  v_ing_neto numeric; v_egr numeric;
  v_estados text[] := array['completada','devuelta_parcial','devuelta_total','cambiada_parcial','cambiada_total'];
begin
  if not private.is_owner() then
    raise exception 'Solo el dueño puede ver el balance';
  end if;

  -- Ingresos: pagos de ventas, atribuidos por la fecha de la VENTA
  select
    coalesce(sum(case when m.metodo='efectivo' then m.monto else 0 end),0),
    coalesce(sum(case when m.metodo='nequi' then m.monto else 0 end),0),
    coalesce(sum(case when m.metodo='daviplata' then m.monto else 0 end),0)
  into v_ef, v_ne, v_da
  from public.metodos_pago_venta m
  join public.ventas v on v.id = m.venta_id
  where v.estado = any(v_estados)
    and (v.created_at at time zone 'America/Bogota')::date between p_desde and p_hasta;

  -- Devoluciones: por la fecha de la VENTA original (consistente con obtener_resumen_dia)
  select coalesce(sum(d.monto_devuelto),0), coalesce(sum(d.monto_cobrado),0)
  into v_reemb, v_cobros
  from public.devoluciones d
  join public.ventas v on v.id = d.venta_id
  where (v.created_at at time zone 'America/Bogota')::date between p_desde and p_hasta;

  -- Egresos: cada uno por su propia fecha de pago (base caja)
  select coalesce(sum(monto_pagado),0) into v_gf
  from public.gastos_fijos_pagos where fecha_pago between p_desde and p_hasta;

  select coalesce(sum(monto),0) into v_gv
  from public.gastos_variables where fecha between p_desde and p_hasta;

  select coalesce(sum(monto),0) into v_prov
  from public.compra_pagos where fecha between p_desde and p_hasta;

  select coalesce(sum(monto),0) into v_sueldos
  from public.empleado_pagos where fecha_pago between p_desde and p_hasta;

  v_ing_neto := v_ef + v_ne + v_da - v_reemb + v_cobros;
  v_egr := v_gf + v_gv + v_prov + v_sueldos;

  return json_build_object(
    'ingresos', json_build_object(
      'efectivo', v_ef, 'nequi', v_ne, 'daviplata', v_da,
      'reembolsos', v_reemb, 'cobros_cambios', v_cobros, 'total_neto', v_ing_neto),
    'egresos', json_build_object(
      'gastos_fijos', v_gf, 'gastos_variables', v_gv,
      'pagos_proveedores', v_prov, 'sueldos', v_sueldos, 'total', v_egr),
    'balance', v_ing_neto - v_egr
  );
end;
$$;

revoke all on function public.obtener_balance(date, date) from public;
grant execute on function public.obtener_balance(date, date) to authenticated;
```

- [ ] **Step 3: Commit** (solo la migración)
```bash
git add supabase/migrations/*_m11_balance.sql
git commit -m "feat(m11): migración — RPC obtener_balance (flujo de caja del período, dueño-only)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Smoke test SQL y correrlo vía MCP

**Files:** Create `supabase/tests/smoke_test_balance.sql` (gitignored)

- [ ] **Step 1: Escribir el smoke** — `begin; ... rollback;`, simulación de auth (claim del dueño), sembrar `auth.users` antes de `public.users`, embeber el RPC `obtener_balance` (aún no aplicado), helpers de assert, centinela `BALANCE_OK_ROLLBACK`. Escenarios:
  1. Setup: dueño (rol='dueno') + empleado. Bajo claim del empleado: venta el 2026-05-15 con pago efectivo 100000 (estado 'completada'). Bajo claim del dueño: gastos_variables 2026-05-20 monto 30000; compra_pagos 2026-05-21 monto 40000; empleado_pagos fecha_pago 2026-05-25 monto 50000; gastos_fijos_pagos 2026-05-10 monto_pagado 20000 (necesita un gasto_fijo padre).
  2. `obtener_balance('2026-05-01','2026-05-31')` ⇒ ingresos.total_neto=100000; egresos.total=140000 (20000+30000+40000+50000); balance=-40000.
  3. **Atribución por fecha de venta:** registrar una devolución HOY (junio) de la venta de mayo (reembolso 100000). `obtener_balance` de MAYO ⇒ reembolsos=100000, total_neto=0 (la devolución resta en el mes de la venta, no en junio). `obtener_balance` de JUNIO ⇒ no incluye ese reembolso.
  4. **Gate:** con claim del empleado, llamar `obtener_balance` ⇒ excepción 'Solo el dueño...'.
  Termina con `raise notice 'BALANCE_OK_ROLLBACK'`.

  > AVISO: la venta debe sembrarse bajo el claim del empleado/dueño que la "registra" (trigger inviolable de `created_by`); los pagos de ventas (`metodos_pago_venta`) y la devolución requieren los datos relacionados. Para la devolución usa el RPC `registrar_devolucion` (ya en el remoto) o inserta directo como service_role dentro de la transacción. Reutiliza el patrón de `smoke_test_devoluciones.sql`/`smoke_test_empleados.sql`.

- [ ] **Step 2: Correr vía MCP** `execute_sql` con el contenido completo. Esperado: `BALANCE_OK_ROLLBACK` sin error. Si falla un assert, corregir migración (Task 1) o smoke. Si hay bug real en la migración, corregirlo en Task 1. No avanzar a Task 3 hasta verde.

---

## Task 3: Aplicar al remoto, regenerar tipos, añadir ruta, commit

- [ ] **Step 1:** `apply_migration` (name `m11_balance`, cuerpo de la migración). Verificar:
```sql
select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and proname='obtener_balance';
```
Esperado 1 fila. Anotar la versión que asigne apply_migration; **renombrar el archivo local a esa versión** (lección M2/M8).
- [ ] **Step 2:** `generate_typescript_types` → sobrescribir `lib/database.types.ts` (no editar a mano).
- [ ] **Step 3:** En `lib/permisos.ts`, añadir `ruta: '/balance'` al módulo `balance`:
```ts
  { id: 'balance',            titulo: 'Balance',            icono: '⚖️', roles: SOLO_DUENO, ruta: '/balance' },
```
- [ ] **Step 4:** `npx tsc --noEmit` → 0 errores.
- [ ] **Step 5: Commit**
```bash
git add lib/database.types.ts lib/permisos.ts supabase/migrations/
git commit -m "feat(m11): aplicar RPC obtener_balance al remoto + regenerar tipos; ruta /balance

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: `lib/balance.ts` — lógica pura (TDD)

**Files:** Create `lib/balance.ts`, `lib/balance.test.ts`

- [ ] **Step 1: Tests que fallan** — `lib/balance.test.ts`:
```ts
jest.mock('./supabase', () => ({ supabase: {} }))

import { proyeccionMes, rangoPeriodo } from './balance'

describe('proyeccionMes', () => {
  it('proyecta linealmente a fin de mes', () => {
    expect(proyeccionMes(500000, 10, 30)).toBe(1500000)
  })
  it('proyecta pérdidas (negativo)', () => {
    expect(proyeccionMes(-200000, 10, 30)).toBe(-600000)
  })
  it('día 0 → 0 (evita división por cero)', () => {
    expect(proyeccionMes(100000, 0, 30)).toBe(0)
  })
})

describe('rangoPeriodo', () => {
  it('mes: primer y último día del mes que contiene la fecha', () => {
    expect(rangoPeriodo('mes', new Date(2026, 1, 15))).toEqual({ desde: '2026-02-01', hasta: '2026-02-28' })
  })
  it('semana: lunes a domingo que contiene la fecha (mié 2026-06-17)', () => {
    expect(rangoPeriodo('semana', new Date(2026, 5, 17))).toEqual({ desde: '2026-06-15', hasta: '2026-06-21' })
  })
})
```

- [ ] **Step 2: Run** `npx jest lib/balance.test.ts` → FAIL.

- [ ] **Step 3: Implementar** `lib/balance.ts` (solo pura):
```ts
function toISO(d: Date): string {
  const y = d.getFullYear()
  const m = String(d.getMonth() + 1).padStart(2, '0')
  const day = String(d.getDate()).padStart(2, '0')
  return `${y}-${m}-${day}`
}

export function proyeccionMes(balanceAcumulado: number, diaActual: number, diasDelMes: number): number {
  if (diaActual <= 0) return 0
  return Math.round((balanceAcumulado / diaActual) * diasDelMes)
}

export function rangoPeriodo(tipo: 'semana' | 'mes', refDate: Date): { desde: string; hasta: string } {
  if (tipo === 'mes') {
    const desde = new Date(refDate.getFullYear(), refDate.getMonth(), 1)
    const hasta = new Date(refDate.getFullYear(), refDate.getMonth() + 1, 0)
    return { desde: toISO(desde), hasta: toISO(hasta) }
  }
  // semana: lunes (1) a domingo (0→7)
  const dow = refDate.getDay() === 0 ? 7 : refDate.getDay()
  const lunes = new Date(refDate.getFullYear(), refDate.getMonth(), refDate.getDate() - (dow - 1))
  const domingo = new Date(lunes.getFullYear(), lunes.getMonth(), lunes.getDate() + 6)
  return { desde: toISO(lunes), hasta: toISO(domingo) }
}
```

- [ ] **Step 4: Run** → PASS. **Step 5: Commit**
```bash
git add lib/balance.ts lib/balance.test.ts
git commit -m "feat(m11): lógica pura de balance (proyección del mes, rangos de período) + tests

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: `lib/balance.ts` — acceso a datos

**Files:** Modify `lib/balance.ts`

- [ ] **Step 1:** Leer `lib/caja.ts` (patrón rpc) y la firma generada de `obtener_balance`. Añadir:
```ts
import { supabase } from './supabase'

export type Balance = {
  ingresos: { efectivo: number; nequi: number; daviplata: number; reembolsos: number; cobros_cambios: number; total_neto: number }
  egresos: { gastos_fijos: number; gastos_variables: number; pagos_proveedores: number; sueldos: number; total: number }
  balance: number
}

export async function obtenerBalance(desde: string, hasta: string): Promise<Balance> {
  const { data, error } = await supabase.rpc('obtener_balance', { p_desde: desde, p_hasta: hasta })
  if (error) throw error
  return data as Balance
}
```
> Si el tipo de retorno del RPC es `Json`, castear a `Balance` (es el contrato del RPC) — no `as any`.

- [ ] **Step 2:** `npx tsc --noEmit` → 0; `npx jest lib/balance.test.ts` → 6 verdes (mock ya está). **Step 3: Commit**
```bash
git add lib/balance.ts
git commit -m "feat(m11): acceso a datos de balance (obtenerBalance vía RPC)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: UI — layout e índice del balance

**Files:** Create `app/(app)/balance/_layout.tsx`, `app/(app)/balance/index.tsx`

- [ ] **Step 1: `_layout.tsx`** (patrón de `app/(app)/empleados/_layout.tsx`, gate `useRequireModulo('balance')`, screen `index` título 'Balance').

- [ ] **Step 2: `index.tsx`:**
  - Estado: `tipo` ('mes'|'semana') + `refDate` (default `new Date()`). Calcula `{desde,hasta}` con `rangoPeriodo` y llama `obtenerBalance`.
  - Selector de período: botones Semana/Mes; flechas ‹ › que mueven `refDate` un mes (o semana) atrás/adelante.
  - Tarjetas: Ingresos netos, Egresos, **Balance** (verde si ≥0, rojo si <0, con etiqueta "Ganancia"/"Pérdida").
  - Desglose de egresos: gastos fijos, variables, proveedores, sueldos (COP formateado).
  - Proyección: solo si el período es el mes en curso (`refDate` en el mes actual y tipo='mes'), mostrar `proyeccionMes(balance, díaActual, díasDelMes)`.
  - Carga/error en español; gate: `if (!requireModulo)` antes de disparar la carga (lección M8). Estilo consistente con pantallas existentes.

- [ ] **Step 3:** `npx tsc --noEmit` → 0; `npm test` → todas verdes. **Step 4: Commit**
```bash
git add "app/(app)/balance/_layout.tsx" "app/(app)/balance/index.tsx"
git commit -m "feat(m11): UI balance — período, totales, desglose de egresos, proyección

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: Tests de UI

**Files:** Create `lib/balance_ui.test.tsx` (patrón de `lib/empleados_ui.test.tsx`)

- [ ] **Step 1:** Mockear `lib/balance`, `lib/auth`, `expo-router`. Cubrir:
  1. **Gating dueño-only:** `_layout` invoca `useRequireModulo('balance')`; guard bloquea cuando devuelve elemento.
  2. **Balance negativo en rojo:** con `obtenerBalance` mockeado a balance<0, la etiqueta dice "Pérdida" / estilo rojo (verifica el texto/estilo real).
  3. **Cambio de período:** tocar Semana/Mes o las flechas vuelve a invocar `obtenerBalance` con nuevas fechas.
  4. **Totales:** se renderizan ingresos netos, egresos y balance del mock.

- [ ] **Step 2:** `npx jest lib/balance_ui.test.tsx` → verde; `npm test` → todas verdes; `npx tsc --noEmit` → 0. **Step 3: Commit**
```bash
git add lib/balance_ui.test.tsx
git commit -m "test(m11): tests de UI de balance (gating, pérdida en rojo, cambio de período, totales)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: Verificación de regresión
- [ ] `npx tsc --noEmit && npm test` → tsc 0, todas verdes.
- [ ] Re-correr `smoke_test_balance.sql` vía MCP → `BALANCE_OK_ROLLBACK`.
- [ ] Read-only vía MCP (bajo claim del dueño real): `select public.obtener_balance('2026-06-01','2026-06-30');` ejecuta sin error y devuelve el JSON.

## Task 9: Code review y cierre
- [ ] **Code review** (superpowers:requesting-code-review) sobre `main..feat/m11-balance`. Foco: gate `is_owner` (ningún no-dueño ve montos), atribución por fecha de venta correcta, agregaciones sin doble conteo, proyección.
- [ ] Actualizar `openspec/changes/tasks.json` (M11 construido) y la memoria `sp9-balance-reportes-state.md`.
- [ ] **Presentar al humano para decisión de merge** (superpowers:finishing-a-development-branch). **No auto-mergear.**

---

## Self-Review (writing-plans)
- **Cobertura del spec:** §2.1/2.2 fórmula+atribución → RPC (Task 1); §2.3 proyección → pura (Task 4); §3 RPC → Tasks 1-3; §4 lib → Tasks 4-5; §5 UI → Task 6 (+ ruta Task 3); §6 pruebas → Tasks 2,4,7,8. Sin huecos.
- **Sin placeholders** en RPC ni lógica pura (código completo). UI con estructura concreta + patrón + referencia a pantallas existentes.
- **Consistencia de tipos:** firma `obtener_balance(date,date)` idéntica en migración, grant y `obtenerBalance`; tipo `Balance` espeja el JSON del RPC; `balance` es el id real del módulo.
- **Orden serial:** RPC (1-3) → lib (4-5) → UI (6-7) → verificación (8) → review/cierre (9). AGENTS.md §5.1.
