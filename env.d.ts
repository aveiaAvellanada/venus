/// <reference types="expo/types" />

// Tipos del entorno Expo (incluye process.env.EXPO_PUBLIC_*) para el typecheck.
// `expo-env.d.ts` es autogenerado por Expo y está en .gitignore; este archivo
// versionado garantiza que `tsc --noEmit` resuelva los tipos sin correr expo antes.
